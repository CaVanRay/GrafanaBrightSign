Ok, making a middleware clone of the website wasn't working out too well, 

so I took a step back to work on a login script and that's showing some progress, just need to get past the new password request that
keeps popping up


Current state of process:

BrightSign loads Grafana
        ↓
login.js injected
        ↓
Check sessionStorage
        ↓
Not complete
        ↓
Wait for login fields
        ↓
admin / admin
        ↓
Log in
        ↓
Forced password change
        ↓
admin / admin
        ↓
Submit
        ↓
Set grafanaAutoLoginComplete = true
        ↓
Grafana dashboard
        ↓
login.js injected again
        ↓
Flag exists
        ↓
SKIP LOGIN
